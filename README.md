# Website of Veepu

<https://veeramaria.fi>

## Development

Static site, no build step. Start the dev server:

```bash
make
```

Serves <http://localhost:8462> via `serve.py`, which mimics GitHub Pages
extensionless URLs (`/veera` → `veera.html`).